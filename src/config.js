// constantes 

/*
export const db = {
    user: process.env.DB_USER || "postgres",
    password: process.env.DB_PASSWORD || "vospodes",
    host: process.env.DB_HOST || "localhost",
    port: process.env.DB_PORT || 5432,
    database: process.env.DB_DATABASE || "inso2db",
  };
  
  export const port = process.env.PORT || 3001;

*/

  //despues lo uso para pasarlo a mi db.js